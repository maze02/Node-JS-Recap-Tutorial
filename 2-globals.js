//GLOBAL - NO WINDOW!!!!

//__dirname - path to current directory
//__filename - file name
// require - function to use modules (common JS)
// module -infro about current module (file)
// process -info about env where the program is being executed

console.log('path to current directory', __dirname)
console.log('filename', __filename)

/*setInterval(() => {
  console.log(
    'functions such as setInterval and setTimeout are acessible to execute in node.js - for example here the console.log is executed as a callback'
  )
}) */
